{
  "name": "trustrag",
  "version": "0.1.0",
  "private": true,
  "description": "Secure enterprise RAG platform",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev:web": "npm run dev --workspace @trustrag/web",
    "build:web": "npm run build --workspace @trustrag/web",
    "typecheck:web": "npm run typecheck --workspace @trustrag/web"
  }
}
